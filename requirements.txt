# Remplacer une dépendance vulnérable
pandas>=2.0.3  # Version sécurisée
numpy>=1.23.5  # Version sécurisée
scikit-learn>=1.5.0  # Mise à jour pour résoudre la fuite de données dans TfidfVectorizer
imbalanced-learn>=0.11.0 
matplotlib==3.7.1
seaborn==0.12.2

# Gestion des modèles et expérimentations
mlflow>=2.16.0  # Mise à jour pour résoudre les vulnérabilités critiques (XSS, LFI, RCE, etc.)
joblib==1.3.1

# Déploiement et API
fastapi>=0.109.1  # Mise à jour pour résoudre les vulnérabilités ReDoS dans python-multipart
uvicorn==0.22.0

# Tests et qualité du code
pytest==7.4.0
pytest-cov==4.1.0
flake8==6.0.0
black>=24.3.0  # Mise à jour pour résoudre la vulnérabilité ReDoS
isort==5.12.0

# Gestion des données déséquilibrées

# Autres utilitaires
python-dotenv==1.0.0